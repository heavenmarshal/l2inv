NLEQ_FLAG = `echo 'library(nleqslv); cat(system.file(package="nleqslv",mustWork=TRUE))'\
| "${R_HOME}/bin/R" --vanilla --slave`/libs/nleqslv.so
PKG_CFLAGS = -DRPRINT  -I include `gsl-config --cflags` ## -UNDEBUG
PKG_LIBS = ${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} `gsl-config --libs` ${NLEQ_FLAG}

#-L/usr/local/lib -lmsRblas -lmsRlapack